export const postLoginFetcher = (payload: any) => ({
	message: "test",
	status: 200,
	data: {},
}); // Create axios instance and use it in fetchers #TODO

export const postRegisterFetcher = (payload: any) => ({
	message: "test",
	status: 200,
	data: {},
}); // Create axios instance and use it in fetchers #TODO
